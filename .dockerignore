# Git / VCS
.git
.gitignore
.gitattributes

# Dependencies (reinstalled inside the image)
node_modules

# Secrets & local data — never bake these into the image
.env
.env.*
data/
.wrangler/

# Issue tracker & agent tooling
.beads/
.agents/
.codex/
.cursor/
.claude/

# Cloudflare Workers apps (the Docker image runs only the Bun target in src/ + packages/core)
apps/

# Tests, docs, CI
tests/
docs/
.github/
*.md

# Build output
dist/
